# noinspection PyShadowingBuiltins,PyUnusedLocal
def compute(x, y):
    """
    Adding two numbers
        Args:
            x (int) : positive integer between 0 - 100
            y (int) : positive integer between 0 - 100
    Returns:
        The sum of two numbers (int)
    """
    return x + y
